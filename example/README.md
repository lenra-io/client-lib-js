# Basic HTML and JS example

To run this example, run the following commands:

```bash
# Install dependencies
npm i
# Build the project
npm run build
# Start the server
npm start
```

Then open http://localhost:3030 in your browser.